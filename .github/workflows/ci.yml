# name: CI Pipeline
# on: [push, pull_request]

# jobs:
#   test:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v2
#       - name: Set up Python 3.x
#         uses: actions/setup-python@v2
#         with:
#           python-version: 3.x
#       - name: Install dependencies
#         run: pip install -r course_10/AIEngineerModelPackaging/requirements.txt
#       - name: Run tests
#         run: pytest course_10/AIEngineerModelPackaging/tests


name: CI/CD Pipeline for Deepfake Model
on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the code
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install test dependencies
        run: pip install pytest pandas

      - name: Run unit tests
        run: pytest tests

#   build:
#     runs-on: ubuntu-latest
#     needs: [test]

#     steps:
#       # Pulls your code from GitHub
#       - name: Check out the code
#         uses: actions/checkout@v4

#       # Enables Docker's advanced build system
#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       # Logs into Docker Hub using credentials stored as secrets in GitHub
#       - name: Log in to Docker Hub
#         run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

#       # Builds the Docker image and pushes it to Docker Hub
#       - name: Build and Push Docker Image
#         run: |
#           docker build -f course_11/docker_example_ci_cd_pipeline/Dockerfile \
#             -t ${{ secrets.DOCKER_USERNAME }}/iris-model:latest \
#             course_11/docker_example_ci_cd_pipeline
#           docker push ${{ secrets.DOCKER_USERNAME }}/iris-model:latest